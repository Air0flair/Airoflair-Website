/** @type {import('next').NextConfig} */
const nextConfig = {
  // You can add custom config here later if needed.
  // For now we keep it minimal so it works everywhere (local + Azure).
};

export default nextConfig;
